{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "init-db": "cd db && knex migrate:latest && knex seed:run",
    "clear-db": "cd db && knex migrate:down"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "knex": "^0.21.5",
    "pg": "^8.3.3",
    "uuid": "^8.3.0"
  }
}
